SSRM можно использовать только при взаимодействии и сервером. Потому что SSRM
основан на постоянном обмене сообщений с сервером о том какие элементы нужно
грузить.

Главное чтобы не было outOfMemory. При этом если получать не
1e5 а 1e6 данных сначала браузер не падает но если начать
отрисовывать данные даже без агрегации то падает. Поэтому
нужно оптимизировать отрисовку таблицы.

Будем использовать treeNode: true.В воркере сформируем сначала
данные для нужной метрики, которые будут хранить только необходимые
данные но с агрегацией. Желательно сразу же закинуть их в indexedDB
чтобы ничего не упало от out outOfMemory. И переопределим функцию рендера
чтобы рендерить только нужные узлы

установить линтер? (exhaustive-deps не показываются)
убрать импорт всех модулей AG-Grid
округление данных
